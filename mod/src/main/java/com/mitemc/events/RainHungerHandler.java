package com.mitemc.events;

/**
 * Superseded — rain hunger is now applied directly by {@link BasalMetabolismHandler} so the
 * baseline + temperature + rain values stay coherent in one place. Kept as an empty marker
 * class for backward compatibility with tutorials/screenshots that mention this handler.
 */
public final class RainHungerHandler {
    private RainHungerHandler() {}
}
